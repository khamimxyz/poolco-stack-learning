type Query {
    allPersons: [Person],
    searchPerson(id: Int): Person
}

type Person {
    id: Int,
    name: String,
    personAddress: PersonAddress
}

type PersonAddress {
    street: String,
    city: String,
    province: String
}